# GitHub Profile README 可视化编辑器使用手册

这份手册用于说明如何维护当前仓库的个人主页 README。

当前仓库已经从“直接手写 README”升级为“配置驱动生成 README”的模式：

```text
profile.config.json
        ↓
tools/generate-readme.js
        ↓
README.md
```

你可以通过可视化编辑器修改内容，也可以直接编辑配置文件，然后一键重新生成 README。

## 目录结构说明

```text
hhhhzx/
├─ README.md                  # GitHub 个人主页最终展示文件
├─ README.md.bak              # 初始备份文件
├─ profile.config.json        # 个人主页数据配置
├─ package.json               # npm 快捷命令
├─ assets/
│  └─ profile-animation.svg   # 本地动态展示 SVG
├─ editor/
│  ├─ index.html              # 可视化编辑器入口
│  ├─ style.css               # 编辑器样式
│  └─ app.js                  # 编辑器逻辑
└─ tools/
   └─ generate-readme.js      # README 生成脚本
```

## 推荐使用方式

如果你只是想新增或修改「代表项目」「联系方式」「关于我」这类内容，推荐使用可视化编辑器。

如果你想批量修改很多内容，或者想精细控制所有展示模块，推荐直接编辑 `profile.config.json`。

## 方式一：使用可视化编辑器

打开这个文件：

```text
editor/index.html
```

也就是：

```text
D:\前端项目练习\46AI-Demo\GitHub\hhhhzx\editor\index.html
```

打开后可以看到几个主要按钮：

| 按钮 | 用途 |
| --- | --- |
| 载入当前配置 | 恢复编辑器内置的当前示例配置 |
| 导入配置 | 导入本地的 `profile.config.json` |
| 复制 README | 把生成后的 README 内容复制到剪贴板 |
| 下载 README.md | 下载生成后的 README 文件 |
| 下载配置 | 下载当前编辑结果对应的配置文件 |

### 编辑代表项目

在「代表项目」区域可以新增、修改、删除项目。

每个项目包含：

| 字段 | 说明 |
| --- | --- |
| 项目名 | 展示在 README 表格里的项目名称 |
| 项目链接 | GitHub 仓库、在线预览地址或其他链接 |
| 简介 | 一句话说明这个项目做什么 |
| 技术栈 | 使用的技术，例如 `Vue / React / Spring Boot` |

如果项目链接留空，README 里会显示纯文本项目名。

### 编辑联系方式

在「联系方式」区域可以新增 GitHub、博客、邮箱、掘金、CSDN 等入口。

每个联系方式包含：

| 字段 | 说明 |
| --- | --- |
| 按钮名称 | 给自己识别用 |
| 链接 | 点击后跳转的地址 |
| 徽章图片链接 | shields.io 或其他徽章图片地址 |
| 图片说明 | 图片的 alt 文本 |

### 编辑完成后怎么保存

编辑器是纯本地页面，不会自动写入仓库文件。

推荐流程：

1. 点击「下载配置」
2. 用下载得到的 `profile.config.json` 替换仓库根目录里的同名文件
3. 在终端运行：

```bash
npm run generate
```

4. 检查生成后的 `README.md`
5. 提交到 Git

如果只是临时想更新 README，也可以点击「复制 README」，然后手动粘贴覆盖 `README.md`。

## 方式二：直接编辑配置文件

打开：

```text
profile.config.json
```

这个文件保存了 README 的所有内容。

常用配置区域：

| 配置项 | 对应 README 区域 |
| --- | --- |
| `profile` | 顶部标题、简介、顶部标签/徽章、打字动画 |
| `about` | 关于我 |
| `contacts` | 联系方式 |
| `skills` | 技术栈 |
| `dynamicShowcase` | 动态展示 SVG |
| `showcaseCard` | 动态 SVG 卡片里的标题、标签、代码行、颜色 |
| `githubData` | GitHub 数据图 |
| `contributionSnake` | 贡献蛇图 |
| `directions` | 项目方向 |
| `projects` | 代表项目 |
| `goals` | 最近想做得更好的事 |
| `footer` | 页脚文案 |

### 修改顶部标签/徽章

顶部这类内容：

```md
<img src="https://img.shields.io/badge/Focus-Full%20Stack-7c3aed?style=flat-square" />
```

对应配置文件里的：

```json
"badges": [
  {
    "label": "Focus Full Stack",
    "src": "https://img.shields.io/badge/Focus-Full%20Stack-7c3aed?style=flat-square",
    "alt": "focus"
  }
]
```

字段说明：

| 字段 | 说明 |
| --- | --- |
| `label` | 给自己识别用的标签名 |
| `src` | 实际展示的图片地址 |
| `alt` | 图片说明文本 |

在可视化编辑器里，可以通过「顶部标签」区域新增、删除或修改。

### 修改技能图标

技能图标这类内容：

```md
<img src="https://skillicons.dev/icons?i=vue,react,ts" />
```

对应配置文件里的：

```json
{
  "title": "Frontend",
  "icons": "vue,react,ts,js,vite,pinia,html,css",
  "alt": "frontend skills"
}
```

字段说明：

| 字段 | 说明 |
| --- | --- |
| `title` | 技能分组标题 |
| `icons` | skillicons 图标 ID，用英文逗号分隔 |
| `alt` | 图片说明文本 |

例如要给工具分组增加 Android Studio，可以把 `icons` 改成：

```text
git,github,docker,kubernetes,vim,vscode,androidstudio
```

在可视化编辑器里，可以通过「技能图标」区域新增、删除或修改分组。

现在编辑器还支持根据 `assets/skill-icons.md` 生成的 Icon ID 列表进行勾选：

- 可以搜索 Icon ID
- 可以勾选/取消勾选
- 会自动更新 `icons` 字段
- 会自动生成类似下面的图片链接并显示预览

```text
https://skillicons.dev/icons?i=git,kubernetes,docker,c,vim
```

如果以后更新了 `assets/skill-icons.md`，可以运行：

```bash
npm run generate:icons
```

它会重新生成：

```text
editor/skill-icons-data.js
```

### 修改动态 SVG 卡片

README 里的动态展示图：

```md
<img src="./assets/profile-animation.svg" alt="animated developer card" width="92%" />
```

现在由 `profile.config.json` 里的 `showcaseCard` 生成。

可以自定义：

| 字段 | 说明 |
| --- | --- |
| `title` | SVG 卡片主标题 |
| `subtitle` | SVG 卡片副标题 |
| `tags` | 卡片里的技术标签 |
| `codeLines` | 右侧终端里的代码行 |
| `colors` | 背景渐变和强调色 |

示例：

```json
"showcaseCard": {
  "title": "Hi, I'm Ananas🍍🍍",
  "subtitle": "Full-stack learner · Frontend explorer",
  "tags": ["Vue", "React", "Spring Boot", "TypeScript"],
  "codeLines": [
    "const dream = idea();",
    "build(dream).ship();",
    "while(true) learn();"
  ],
  "colors": {
    "backgroundStart": "#0F172A",
    "backgroundMiddle": "#111827",
    "backgroundEnd": "#312E81",
    "accent": "#38BDF8",
    "accent2": "#A78BFA",
    "accent3": "#F472B6"
  }
}
```

修改后运行：

```bash
npm run generate
```

这个命令会先重新生成：

```text
assets/profile-animation.svg
```

再重新生成：

```text
README.md
```

### 新增代表项目示例

在 `projects` 数组里新增一项：

```json
{
  "name": "AI Demo",
  "url": "https://github.com/hhhhzx/your-repo",
  "description": "一个 AI 相关练习项目，用来探索智能交互和前端应用结合。",
  "tech": "Vue / React / OpenAI API"
}
```

注意：

- JSON 字符串必须使用英文双引号
- 每一项之间需要英文逗号
- 最后一项后面不要多写逗号

## 生成 README

修改 `profile.config.json` 后，运行：

```bash
npm run generate
```

或者直接运行：

```bash
node tools/generate-readme.js
```

成功后终端会显示：

```text
Generated README.md from profile.config.json
```

这表示 README 已经根据配置重新生成。

## 日常维护流程

推荐以后按这个节奏维护：

```text
修改 profile.config.json
        ↓
npm run generate
        ↓
预览 README.md
        ↓
git add .
        ↓
git commit
        ↓
git push
```

## 常见问题

### 1. 为什么我在编辑器里改了，但 README 没变？

编辑器是本地静态页面，不会直接写入磁盘文件。

你需要下载配置或复制 README，然后手动替换文件。

如果你修改的是 `profile.config.json`，还需要运行：

```bash
npm run generate
```

### 2. 图片不显示怎么办？

README 里有些图片来自外部服务，例如：

- `skillicons.dev`
- `shields.io`
- `github-readme-activity-graph.vercel.app`
- `raw.githubusercontent.com`

如果偶尔不显示，可能是网络、缓存或第三方服务暂时不可用。

当前本地动画 `assets/profile-animation.svg` 不依赖外部服务，稳定性更高。

### 3. GitHub 页面没有更新怎么办？

可以检查：

1. 是否已经重新生成 `README.md`
2. 是否已经提交并推送到 GitHub
3. 仓库名是否仍然是 `hhhhzx/hhhhzx`
4. GitHub 页面是否存在缓存，等待一会儿再刷新

### 4. JSON 报错怎么办？

通常是这些原因：

- 少了英文逗号
- 多了尾随逗号
- 使用了中文引号
- 字符串没有用英文双引号包起来

可以先恢复到上一次正常的配置，再逐步修改。

## 小建议

代表项目区域建议只放 3～5 个最能体现能力的项目。

每个项目最好写清楚：

- 项目解决了什么问题
- 你负责或练习了什么能力
- 使用了哪些技术
- 有没有在线预览或截图

这样 Profile README 不只是“好看”，还会更像一个轻量作品集。
